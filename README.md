# finn-loop-smoketest

Throwaway repository for smoke-testing the **Finn loop** (`finn-spec` → `finn-build` → `finn-review`).

- Linear team: `MOR`
- CI: `node --test` via GitHub Actions (required check: `test`)

Safe to delete once the loop is verified.
